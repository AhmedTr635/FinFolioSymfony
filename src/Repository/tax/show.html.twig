{% extends 'base.html.twig' %}

{% block title %}Tax{% endblock %}

{% block body %}
    <h1>Tax</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tax.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ tax.montant }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ tax.type }}</td>
            </tr>
            <tr>
                <th>Optimisation</th>
                <td>{{ tax.optimisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tax_index') }}">back to list</a>

    <a href="{{ path('app_tax_edit', {'id': tax.id}) }}">edit</a>

    {{ include('tax/_delete_form.html.twig') }}
{% endblock %}

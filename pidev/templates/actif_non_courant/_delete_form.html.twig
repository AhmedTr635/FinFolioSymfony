<form method="post" action="{{ path('app_actif_non_courant_delete', {'id': actif_non_courant.id}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actif_non_courant.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_real_estate_delete', {'id': real_estate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ real_estate.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

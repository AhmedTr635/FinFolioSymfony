{{ form_start(form) }}

<div class="mb-3 form-group" style="display: none;">
    <label class="required" for="credit1_user_id">User id</label>
    {{ form_widget(form.user_id, {'attr': {'class': 'form-control'}}) }}
</div>
    {{ form_errors(form) }}

    {{ form_widget(form) }}

<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
